<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Blog Maino</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'Posts',
                      posts_path,
                      class: "nav-link #{active_class(['posts'])}" %>
        </li>
        <li class="nav-item">
          <%= link_to 'Tags',
                      tags_path,
                      class: "nav-link #{active_class(['tags'])}" %>
        </li>

        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Your Profile',
                        user_path(current_user),
                        class: "nav-link #{active_class(%w[users devise/registrations])}" %>
          </li>
        <% end %>
      </ul>
      <form class="d-flex">
        <% if user_signed_in? %>
          <%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete }, class: 'btn btn-outline-light' %>
        <% else %>
        <%= link_to 'Sign In', new_user_session_path, class: 'btn btn-outline-light' %>
        <% end %>
      </form>
    </div>
  </div>
</nav>