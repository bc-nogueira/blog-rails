<% if flash.any? %>
  <div class="mt-3">
    <% flash.each do |type, msg| %>
      <div class="alert alert-<%= type == 'notice' ? 'success' : 'danger' %>">
        <%= msg %>
      </div>
    <% end %>
  </div>
<% end %>